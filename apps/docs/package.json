{
  "name": "@unuxt/docs",
  "private": true,
  "scripts": {
    "dev": "nuxt dev --extends docus --port 3001",
    "build": "nuxt build --extends docus",
    "generate": "nuxt generate --extends docus",
    "preview": "nuxt preview --extends docus",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@nuxt/ui": "^4.4.0",
    "better-sqlite3": "^12.5.0",
    "docus": "latest",
    "nuxt": "^4.2.2"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
