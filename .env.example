# ===========================================
# Universal Nuxt 4 Starter Template
# Environment Variables Configuration
# ===========================================
#
# Copy this file to .env and fill in your actual values
# ⚠️  NEVER commit .env to version control
#
# Legend:
#   [REQUIRED] - Must be set for the app to start
#   [OPTIONAL] - Feature-specific, app works without it
#   [RECOMMENDED] - Optional but highly recommended for production
#
# ===========================================

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# DATABASE CONFIGURATION
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# [REQUIRED] PostgreSQL database connection string
# Format: postgresql://user:password@host:port/database
# Example: postgresql://postgres:mypassword@localhost:5432/unuxt_dev
# Production: Use connection pooling (e.g., Supabase, Neon, Railway)
DATABASE_URL=postgresql://postgres:password@localhost:5432/unuxt

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# AUTHENTICATION (Better Auth)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# [REQUIRED] Secret key for encrypting sessions and tokens
# MUST be at least 32 characters long
# Generate with: openssl rand -base64 32
# ⚠️  CHANGE THIS IN PRODUCTION - do not use the example value
BETTER_AUTH_SECRET=your-secret-key-min-32-characters-long-change-this-in-production

# [REQUIRED] Application URL for OAuth callbacks and email links
# Development: http://localhost:3000
# Production: https://yourdomain.com (no trailing slash)
BETTER_AUTH_URL=http://localhost:3000

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# OAUTH PROVIDERS (Optional)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# [OPTIONAL] Google OAuth
# Get credentials: https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: {BETTER_AUTH_URL}/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# [OPTIONAL] GitHub OAuth
# Get credentials: https://github.com/settings/developers
# Authorization callback URL: {BETTER_AUTH_URL}/api/auth/callback/github
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# IMAGE UPLOADS (Cloudinary)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# [OPTIONAL] Cloudinary configuration for image uploads
# Get credentials: https://console.cloudinary.com/
# Used for: User avatars, organization logos
# If not configured, upload features will be disabled
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# EMAIL (SMTP)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# [RECOMMENDED] SMTP configuration for sending emails
# Used for: Password resets, email verification, magic links, org invitations
# If not configured, emails will be logged to console (development only)
#
# Common SMTP Providers:
# ┌──────────┬────────────────────────────────────┬──────┬─────────────────┐
# │ Provider │ Host                               │ Port │ Notes           │
# ├──────────┼────────────────────────────────────┼──────┼─────────────────┤
# │ Gmail    │ smtp.gmail.com                     │ 587  │ App password    │
# │ SendGrid │ smtp.sendgrid.net                  │ 587  │ API key as pass │
# │ Mailgun  │ smtp.mailgun.org                   │ 587  │ Domain config   │
# │ AWS SES  │ email-smtp.{region}.amazonaws.com  │ 587  │ IAM credentials │
# │ Postmark │ smtp.postmarkapp.com               │ 587  │ Server token    │
# │ Resend   │ smtp.resend.com                    │ 587  │ API key         │
# └──────────┴────────────────────────────────────┴──────┴─────────────────┘

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Email sender information
FROM_EMAIL=noreply@example.com
FROM_NAME=Unuxt

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# APPLICATION SETTINGS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# [OPTIONAL] Node environment (auto-detected in most cases)
# Options: development | production | test
# Default: development
NODE_ENV=development

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# TESTING (for CI/CD and local testing)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# For testing, you can use a separate .env.test file
# See .env.test.example for test-specific configuration

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# QUICK START CHECKLIST
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# Minimum required for local development:
# 1. ✅ Set DATABASE_URL (PostgreSQL connection)
# 2. ✅ Generate and set BETTER_AUTH_SECRET (32+ chars)
# 3. ✅ Set BETTER_AUTH_URL (http://localhost:3000)
#
# Recommended for full functionality:
# 4. 📧 Configure SMTP for email sending
# 5. 🖼️  Configure Cloudinary for image uploads
# 6. 🔐 Add OAuth providers (Google, GitHub)
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
